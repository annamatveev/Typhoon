// import React from 'react';
// import { shallow } from 'enzyme';

// import TransactionsListView from '../index';

describe('<TransactionsListView />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
